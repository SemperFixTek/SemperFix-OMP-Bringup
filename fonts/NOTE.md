# NOTEs for FONTS usage.

## The current font is Jebrains Mono, however we might use Fira Code as an alternate.  Both stanadard release packages are included in this distro, but we'll probably trim the list once we figure out which fonts we want to keep.